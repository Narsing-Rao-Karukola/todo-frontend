export type ssoDtoType = { username: string; password: string };
export type todoDtoType = { title: string; completed: boolean };
export type TodoType = {
  id: number;
  title: string;
  completed: boolean;
};
